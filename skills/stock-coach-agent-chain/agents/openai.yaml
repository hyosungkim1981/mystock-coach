interface:
  display_name: "주식 코칭 에이전트 체인"
  short_description: "리서치부터 검증까지 체인 실행"
  default_prompt: "Use $stock-coach-agent-chain to run holdings through research, analyst reporting, portfolio operation, validation, and dashboard JSON."
policy:
  allow_implicit_invocation: true
