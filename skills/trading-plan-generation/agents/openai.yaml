interface:
  display_name: "트레이딩 계획 생성"
  short_description: "진입 청산 손절 트레일링스톱"
  default_prompt: "Use $trading-plan-generation to create a trading plan with entry, exit, stop loss, trailing stop, invalidation, and risk notes."
policy:
  allow_implicit_invocation: true
