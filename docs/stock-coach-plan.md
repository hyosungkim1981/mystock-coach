# 국내주식 포트폴리오 코칭 봇 기획

## 목표

국내주식 보유 현황을 MTS 화면 캡처 이미지에서 읽어오고, 종목별 평단가, 현재가, 수익률, 비중을 계산해 매수/보유/매도 액션 단계를 대시보드로 보여준다.

이 도구는 투자 결정을 대신하지 않고, 보유 종목을 점검하기 위한 코칭 보조 도구로 설계한다.

## 1차 MVP 범위

- MTS 캡처 이미지 업로드
- 브라우저 OCR 시도
- OCR 결과 텍스트 보정 입력
- 종목명, 종목코드, 수량, 평단가, 현재가, 수익률, 비중 파싱
- 파싱된 보유 종목 직접 수정
- 종목별 액션 점수 산출
- 적극매수, 분할매수 1단계, 분할매수 2단계, 보유, 분할매도 1단계, 분할매도 2단계, 적극매도 단계 표시
- 포트폴리오 요약 대시보드

## 입력 데이터

MTS 이미지 OCR에서 우선 인식할 항목은 다음과 같다.

- 종목명
- 종목코드
- 수량
- 평단가
- 현재가 또는 평가단가
- 수익률
- 비중

OCR이 틀릴 수 있으므로 결과 텍스트와 표 데이터를 모두 사용자가 수정할 수 있어야 한다.

## 액션 점수 체계

액션 점수는 0-100%로 표현한다. 100에 가까울수록 매수 쪽, 0에 가까울수록 매도 쪽 신호로 본다.

| 점수 범위 | 액션 단계 | 의미 |
|---:|---|---|
| 85-100 | 적극매수 | 모멘텀, 수익률, 리스크가 모두 우호적 |
| 72-84 | 분할매수 2단계 | 매수 신호가 강하지만 단계적으로 접근 |
| 60-71 | 분할매수 1단계 | 관심 매수 구간, 소액/관찰 중심 |
| 45-59 | 보유 | 추가 매수/매도보다 관찰 우선 |
| 33-44 | 분할매도 1단계 | 리스크 일부 축소 검토 |
| 20-32 | 분할매도 2단계 | 비중 축소 필요성이 큼 |
| 0-19 | 적극매도 | 손실/리스크/뉴스 악화가 큰 상태 |

## 점수 계산 초안

1차 MVP는 MTS 보유 정보만으로 계산하고, 뉴스와 회사 정보는 수동 점수 또는 후속 API 연결 대상으로 둔다.

- 가격 모멘텀: 수익률 기반
- 뉴스 모멘텀: 기본 0점, 사용자가 종목별로 -100부터 100까지 조정
- 비중 리스크: 포트폴리오 내 비중이 높을수록 추가매수 점수를 낮춤
- 손익 리스크: 큰 손실 또는 과도한 급등은 보수적으로 반영
- 포지션 품질: 적정 수익률과 관리 가능한 비중을 우호적으로 평가

## 후속 확장

- 네이버 금융/증권 뉴스 또는 별도 뉴스 API 연결
- DART 공시 연결
- 종목별 회사 개요 자동 요약
- 장 시작 전/장 마감 후 일일 리포트 생성
- 텔레그램, 카카오, 이메일 알림
- 사용자별 목표 비중, 손절선, 익절선 설정
- OCR 인식률 개선을 위한 MTS별 템플릿 파서

## 2026-06-27 MVP 구현 내용

- 설치 없이 실행 가능한 정적 웹 대시보드 생성
- MTS 이미지 업로드 UI와 미리보기 구현
- 브라우저 OCR 실행 버튼 추가
- OCR 실패 또는 오인식 시 직접 텍스트 보정 가능
- OCR/보정 텍스트에서 국내주식 보유 행 파싱
- 종목명, 종목코드, 수량, 평단가, 현재가, 수익률, 비중 추출
- 종목별 뉴스 점수 수동 조정 슬라이더 추가
- 0-100% 액션 점수와 7단계 액션 라벨 계산
- 총 평가금액, 가중 수익률, 평균 액션, 최대 비중 요약 카드 구현
- 액션 단계별 종목 수 분포 차트 구현
- 표에서 보유 정보를 직접 수정하면 점수 즉시 재계산
- 샘플 입력 버튼으로 데모 데이터 확인 가능

## 2026-06-27 검증 내용

- 계산 로직 단위 테스트 작성
- OCR 텍스트 파싱 테스트 작성
- 이미지 파일 검증 테스트 작성
- 빈 포트폴리오 중립 표시 테스트 작성
- 테스트 5개 통과
- 라인 커버리지 96.86%
- 브라우저에서 샘플 입력, 요약 카드, 액션 분포, 종목 표 렌더링 확인
- 1280px 화면에서 가로 넘침 없음 확인

## 2026-06-27 MTS 샘플 이미지 디버깅

제공된 MTS 캡처는 `종목명 / 수익률 / 평가손익 / 평가금액` 화면이다. 기존 MVP 파서는 `종목명 / 종목코드 / 수량 / 평단가 / 현재가 / 수익률 / 비중`이 한 줄에 있는 텍스트를 우선 지원했기 때문에 이 화면을 온전히 읽지 못했다.

수정 내용:

- `평가손익 / 평가금액` 기반 보유 화면 파서 추가
- OCR 결과가 행 단위로 나오거나 컬럼 단위로 나와도 읽을 수 있게 처리
- 평가금액 합계 기준으로 종목별 비중 자동 계산
- 표에 평가금액 컬럼 추가
- 수량/평단가가 이미지에 없는 경우 사용자 메시지로 안내
- `file://`로 열었을 때 OCR 엔진 제한 가능성 안내

제한 사항:

- 제공된 이미지에는 수량과 평단가가 보이지 않으므로, 이 화면만으로 평단가를 계산할 수 없다.
- 평단가까지 자동 입력하려면 MTS에서 수량/평균단가 컬럼이 보이는 화면 캡처가 필요하다.
- 이미지 OCR은 브라우저 보안 정책 때문에 파일을 직접 여는 방식보다 로컬 서버 주소로 여는 방식이 안정적이다.

## 2026-06-27 다중 이미지 및 종목별 근거 설명

추가 구현 내용:

- 이미지 선택에서 2개 이상의 파일을 동시에 선택 가능하도록 변경
- 드래그 앤 드롭도 여러 이미지 파일을 받을 수 있도록 변경
- 선택한 이미지들을 여러 썸네일로 미리보기
- OCR 실행 시 선택된 이미지를 순서대로 읽고 결과 텍스트를 하나로 합침
- 다중 이미지 업로드에도 파일 형식과 용량 검증 적용
- 종목별 코칭 표에 `내용` 컬럼 추가
- `내용` 버튼 클릭 시 해당 종목의 점수와 액션 근거를 텍스트로 펼침
- 근거 텍스트에 수익률, 비중, 뉴스 점수, 모멘텀/리스크/비중 세부 신호 포함

검증 내용:

- 다중 이미지 파일 검증 테스트 추가
- 종목별 액션 설명 테스트 추가
- 브라우저에서 `multiple` 파일 선택 속성 확인
- 브라우저에서 샘플 입력 후 `내용` 버튼 클릭 및 근거 설명 표시 확인

## 2026-06-27 종목별 전략 설명 고도화

추가 구현 내용:

- 종목별 `성장성` 점수 슬라이더 추가
- 액션 점수 계산에 성장성 신호 반영
- `내용` 클릭 시 기존 점수 근거 외에 성장성 판단, 매수 전략, 매도 전략을 함께 표시
- 현재가가 있는 종목은 추천 매수가, 2차 매수가, 추천 매도가, 일부 매도 기준, 손절 기준을 계산
- 현재가가 없는 종목은 추천 가격 산출이 불가한 이유와 필요한 입력값을 안내
- 추천 가격은 확정 매매 지시가 아니라 현재가 기준의 기계적 분할 접근 기준가로 표시

제한 사항:

- 아직 실시간 회사정보, 재무제표, 뉴스 API가 연결되어 있지 않으므로 성장성 점수는 사용자가 직접 조정한다.
- 실제 기업 성장성 자동 판단은 DART, 뉴스, 재무 데이터 연동 후 고도화한다.
- 매수/매도 추천가는 현재가 기반의 참고 기준이며 투자 판단을 대체하지 않는다.

## 2026-06-27 종목별 코칭 표 우선순위 변경

변경 내용:

- 종목별 코칭 표에서 `점수 / 액션 / 내용`을 앞쪽으로 이동
- 현재 순서: `종목 / 점수 / 액션 / 내용 / 코드 / 수량 / 평단가 / 현재가 / 수익률 / 비중 / 평가금액 / 뉴스 / 성장성`
- 의사결정에 필요한 요약 정보를 먼저 보고, 세부 입력값은 뒤에서 조정하도록 개선

검증 내용:

- 헤더 순서 테스트 추가
- 브라우저에서 샘플 입력 후 첫 행 순서 확인

## 2026-06-29 카카오톡 연동 검토

목표:

- 사용자가 카카오톡에서 MTS 잔고 이미지를 보내면 종목별 코칭 대시보드를 확인할 수 있게 한다.
- 가능하면 오픈채팅방 안에서 자연스럽게 결과를 공유한다.

공식 API 검토 결과:

- 카카오 디벨로퍼스 웹훅은 계정 연결 해제, 계정 상태 변경, 카카오톡 공유 전송 성공, 카카오톡 채널 추가/차단 같은 이벤트 중심이다.
- 카카오톡 공유는 사용자가 서비스 화면에서 공유할 내용을 선택한 뒤 카카오톡 친구 또는 채팅방으로 보내는 기능이다.
- 카카오톡 메시지 API는 같은 서비스 사용자 또는 친구에게 메시지를 보내는 목적이며, 서비스가 일반 오픈채팅방 메시지와 이미지를 직접 수신하는 공식 입력 채널로 보기 어렵다.
- 카카오톡 채널 챗봇의 스킬 서버는 봇 시스템에서 HTTP POST 요청을 받아 응답하는 구조로, 오픈채팅방이 아니라 카카오톡 채널 기반으로 검토해야 한다.

권장 방향:

1. 1단계는 웹 업로드 중심으로 유지한다.
   - 오픈채팅방에는 `대시보드 만들기` 링크를 고정 공지 또는 버튼으로 제공한다.
   - 사용자는 링크를 열어 MTS 이미지를 업로드한다.
   - OCR, 점수 계산, 대시보드 생성은 현재 웹앱 또는 서버에서 처리한다.
   - 결과 화면에 `카카오톡 공유` 버튼을 추가해 오픈채팅방에 결과 링크를 다시 공유한다.

2. 2단계는 카카오톡 채널 챗봇을 별도 검토한다.
   - 카카오톡 채널을 만들고 챗봇 관리자센터 스킬 서버를 연결한다.
   - 사용자가 채널 챗봇에 이미지를 보내면 서버가 이미지 URL 또는 입력값을 받아 OCR 작업을 큐에 넣는다.
   - 챗봇 응답은 즉시 처리 가능한 요약 또는 대시보드 링크로 반환한다.
   - 스킬 응답 시간 제한이 있으므로 OCR과 뉴스 분석은 백그라운드 작업으로 분리하는 설계가 필요하다.

3. 일반 카카오톡 PC 자동화 또는 화면 스크래핑은 제외한다.
   - 공식 API가 아니므로 안정성, 약관, 보안, 개인정보 리스크가 크다.
   - 잔고 이미지에는 계좌/자산 정보가 포함되므로 비공식 자동화는 서비스화하지 않는다.

필요한 서버 API 초안:

- `POST /api/v1/portfolio-sessions`: 이미지 업로드 또는 이미지 URL을 받아 분석 작업 생성
- `GET /api/v1/portfolio-sessions/{id}`: 분석 상태와 결과 조회
- `POST /api/v1/portfolio-sessions/{id}/share-links`: 카톡 공유용 만료 링크 생성
- `DELETE /api/v1/portfolio-sessions/{id}`: 사용자가 업로드 이미지와 분석 결과 삭제

보안 원칙:

- 업로드 이미지는 파일 형식, 확장자, 용량을 검증한다.
- 분석 결과 링크는 추측 불가능한 토큰과 만료 시간을 둔다.
- 잔고 이미지 원본은 기본 24시간 이내 자동 삭제한다.
- 카카오 앱 키, OCR API 키, 뉴스 API 키는 환경 변수로 관리한다.
- 로그에는 종목명 정도만 남기고 계좌번호, 이름, 이미지 URL, 원본 OCR 전문은 남기지 않는다.

참고 문서:

- Kakao Developers Webhook: https://developers.kakao.com/docs/en/getting-started/callback
- Kakao Talk Share: https://developers.kakao.com/docs/en/kakaotalk-share/common
- Kakao Talk Message API: https://developers.kakao.com/docs/en/kakaotalk-message/common
- Kakao Talk Channel REST API: https://developers.kakao.com/docs/en/kakaotalk-channel/rest-api
- Kakao Business Chatbot Skill: https://kakaobusiness.gitbook.io/main/tool/chatbot/skill_guide/make_skill

## 2026-06-29 카카오톡 공유형 MVP 구현

구현 내용:

- 왼쪽 입력 패널에 `카톡방 공유` 영역 추가
- `방 공지 링크 복사` 버튼 추가
  - 오픈채팅방 공지에 붙일 대시보드 만들기 링크를 복사한다.
  - 현재 로컬 주소에서는 다른 기기에서 열리지 않을 수 있으므로, 배포 후 고정 공지 링크로 사용한다.
- `결과 요약 공유` 버튼 추가
  - 종목을 표로 변환하면 활성화된다.
  - 카카오 JavaScript SDK와 앱 키가 설정되어 있으면 카카오톡 공유 템플릿을 호출한다.
  - 설정이 없으면 브라우저 공유 기능을 우선 사용하고, 불가능하면 공유 문구를 클립보드에 복사한다.
- 공유 메시지는 개인정보 보호를 위해 종목명, 종목코드, 총평가금액, 원본 OCR 텍스트를 포함하지 않는다.
- 공유 메시지에는 `점검 종목 수`, `평균 액션`, `가중 수익률`, `대시보드 링크`만 포함한다.
- 공유 링크는 URL의 query/hash를 제거해 OCR 전문이나 화면 상태가 의도치 않게 공유되지 않도록 했다.

후속 배포 작업:

- 공개 HTTPS 배포 주소 준비
- 카카오 디벨로퍼스 앱 생성
- JavaScript 키를 배포 환경 설정으로 주입
- 카카오 플랫폼 Web 도메인에 배포 도메인 등록
- 필요 시 서버형 `portfolio-sessions` API로 상세 대시보드 공유 링크 생성
- 원본 이미지와 분석 결과의 자동 삭제 정책 적용

## 2026-06-29 공개 HTTPS 배포

배포 결과:

- GitHub 공개 저장소 생성: https://github.com/hyosungkim1981/mystock-coach
- GitHub Pages 공개 주소 생성: https://hyosungkim1981.github.io/mystock-coach/
- HTTPS 응답 확인 완료
- GitHub Pages source는 `main` 브랜치 `/` 경로를 사용한다.

배포 전 검증:

- `npm test` 통과
- `npm run build` 통과
- GitHub push 전 ECC pre-push 검증에서 테스트와 빌드 통과

메모:

- Sites 배포도 검토했지만 현재 워크스페이스에서 Sites 기능이 활성화되어 있지 않아 GitHub Pages로 우회 배포했다.
- 카톡 오픈채팅방 공지 링크는 로컬 주소가 아니라 위 GitHub Pages 주소를 사용한다.
- 추후 카카오 JavaScript SDK를 실제 연결하려면 카카오 디벨로퍼스 Web 플랫폼 도메인에 `https://hyosungkim1981.github.io`를 등록한다.

## 2026-07-17 에이전트 설계 시작

첫 번째 에이전트로 `주식 리서치 전문가`를 정의했다.

- 정의 파일: `docs/agents/stock-research-expert.md`
- 역할: 종목별 회사 개요, 뉴스, 공시, 성장성, 리스크를 조사해 대시보드 점수 산정 근거를 제공
- 대시보드 연동값: 성장성 점수, 뉴스 모멘텀 점수, 리스크 등급, 액션 참고값
- 개인정보 원칙: 계좌번호, 실명, 잔고 이미지 원본, OCR 전문을 요구하지 않음
- 투자 안전 원칙: 확정 매매 지시가 아니라 리서치 참고 자료로 제공

워크스페이스 에이전트 생성 도구는 현재 `Not found`로 응답해 바로 등록하지 못했다. 기능이 활성화되면 위 정의 파일의 시스템 지시문을 그대로 사용해 Workspace Agent로 등록한다.

두 번째 에이전트로 `주식 애널리스트`를 정의했다.

- 정의 파일: `docs/agents/stock-analyst.md`
- 역할: 리서치 전문가의 조사 결과를 바탕으로 산업/경제 분석, 재무 분석, 적정 주가 산정, 투자의견 리포트 작성
- 대시보드 연동값: 애널리스트 의견, 목표주가, 적정가치 범위, 매수/매도 구간, 밸류에이션 점수, 리스크 점수
- 목표주가 방식: PER, PBR, EV/EBITDA, DCF, SOTP, 피어 비교
- 투자 안전 원칙: 목표주가와 투자의견은 리서치 참고 자료이며 개인 맞춤 투자 자문으로 표현하지 않음

세 번째 에이전트로 `포트폴리오 운영자`를 정의했다.

- 정의 파일: `docs/agents/portfolio-operator.md`
- 역할: 애널리스트 자료를 바탕으로 보유 종목 목표 비중, 현금 비중, 리밸런싱 계획을 제안
- 대시보드 연동값: 목표 비중, 리밸런싱 액션, 우선순위, 목표 비중 차이, 현금 계획, 운영 코멘트
- 리밸런싱 액션: 비중확대, 소폭확대, 유지, 일부축소, 대폭축소, 정리검토
- 투자 안전 원칙: 실제 주문 실행 없이 사용자가 검토할 수 있는 포트폴리오 운영 참고 자료만 제공

에이전트 체인:

1. `주식 리서치 전문가`: 원천 자료 조사
2. `주식 애널리스트`: 투자의견, 목표주가, 밸류에이션 분석
3. `포트폴리오 운영자`: 목표 비중과 리밸런싱 운영안 생성

트레이딩 스타일별 에이전트 3종을 추가 정의했다.

- `데이트레이딩 에이전트`
  - 정의 파일: `docs/agents/day-trading-agent.md`
  - 역할: 당일 매수·당일 청산 원칙의 장중 기술적 분석과 트레일링 스톱 계획 수립
  - 대시보드 연동값: 당일 진입 신호, 진입 구간, 최초 손절가, 트레일링 스톱 규칙, 당일 목표가, 오버나잇 금지
- `스윙 트레이딩 에이전트`
  - 정의 파일: `docs/agents/swing-trading-agent.md`
  - 역할: 며칠~몇 주 보유를 전제로 일봉/주봉 기술적 분석, 분할매수, 분할매도, 트레일링 스톱 계획 수립
  - 대시보드 연동값: 스윙 신호, 1차 매수 구간, 추가 매수 구간, 손절가, 목표 구간, 예상 보유 기간
- `프라이스 액션 스윙 트레이딩 에이전트`
  - 정의 파일: `docs/agents/price-action-swing-trading-agent.md`
  - 역할: 보조지표를 배제하고 캔들 패턴, 시장 구조, 지지/저항만으로 스윙 시나리오 분석
  - 대시보드 연동값: 프라이스 액션 신호, 시장 구조 방향, 진입 트리거, 무효화 가격, 트레일링 스톱 규칙, 패턴 요약

트레이딩 에이전트 공통 안전 원칙:

- 실제 주문은 실행하지 않는다.
- 모든 매매 아이디어는 투자 자문이 아니라 참고용 트레이딩 계획으로 제공한다.
- 손절 없는 물타기, 미수, 신용, 과도한 레버리지, 집중 매수를 권하지 않는다.
- 진입 조건, 청산 조건, 실패 시나리오, 트레일링 스톱 기준을 항상 함께 제시한다.

에이전트 품질 검증을 위한 `에이전트 검증 관리자`를 추가 정의했다.

- 정의 파일: `docs/agents/agent-validation-manager.md`
- 역할: 각 에이전트 답변의 형식, 근거, 안전성, 출처, 대시보드 연동 가능성을 검증
- 검증 등급: PASS, WARN, FAIL
- 주요 검증 항목: 출력 형식, 출처/기준일, 숫자 근거, 금융 안전성, 개인정보 보호, 대시보드 필드 누락, 에이전트 간 모순
- 대시보드 연동값: 검증 상태, 검증 점수, 누락 필드, 안전성 경고, 수정 요청 프롬프트, 대시보드 사용 가능 여부

에이전트 검증을 체계화하기 위한 `에이전트 테스트 시나리오 생성기`를 추가 정의했다.

- 정의 파일: `docs/agents/agent-test-scenario-generator.md`
- 역할: 각 에이전트를 테스트할 정상/경계/위험/안전성/형식 시나리오 생성
- 산출물: 입력 프롬프트, 제공 데이터, 필수 필드, PASS/WARN/FAIL 조건, 안전성 함정, 대시보드 필드
- 검증 흐름: 테스트 시나리오 생성기 → 대상 에이전트 답변 → 에이전트 검증 관리자 채점 → WARN/FAIL 시 수정 요청
- 목적: 에이전트가 늘어나도 같은 기준으로 회귀 테스트와 품질 검증을 수행

`에이전트 테스트 시나리오 생성기`를 기준으로 첫 번째 테스트 세트를 생성했다.

- 실행 결과 파일: `docs/agent-test-runs/2026-07-17-first-scenario-set.md`
- 테스트 성격: 전체 에이전트 스모크 테스트
- 시나리오 수: 7개
- 테스트 대상: 주식 리서치 전문가, 주식 애널리스트, 포트폴리오 운영자, 데이트레이딩, 스윙 트레이딩, 프라이스 액션 스윙 트레이딩, 에이전트 검증 관리자
- 우선 검증 항목: 위험한 매매 조언 차단, 오버나잇 금지 준수, 비중 과다 관리, 근거 없는 목표주가 방지, 손절 없는 물타기 방지
- 다음 단계: 각 시나리오를 대상 에이전트에 전달하고, 답변을 `에이전트 검증 관리자`가 PASS/WARN/FAIL로 채점한다.

## 2026-07-17 다음 진행 우선순위

현재는 에이전트 정의와 테스트 시나리오 생성까지 완료된 상태다. 다음은 실제 앱에 붙이기 전에 작은 단위의 실행 루프를 검증하는 것이 우선이다.

권장 진행 순서:

1. `1개 종목 end-to-end 에이전트 실행 샘플`을 만든다.
   - 예시 종목: 삼성전자 또는 NAVER
   - 흐름: 보유 종목 데이터 → 주식 리서치 전문가 → 주식 애널리스트 → 포트폴리오 운영자 → 에이전트 검증 관리자
   - 산출물: 종목별 점수, 액션, 매수/매도 참고 구간, 성장성, 리스크, 검증 상태
   - 목적: 에이전트 체인이 실제 대시보드 데이터 형태로 이어지는지 확인

2. `에이전트 검증 관리자`로 첫 테스트 시나리오를 채점한다.
   - 우선 대상: `VALIDATOR-SAFETY-001`, `DAY-NO-OVERNIGHT-001`, `OPERATOR-CONCENTRATION-001`
   - 목적: 위험한 매매 조언, 오버나잇 권유, 비중 과다 방치 같은 치명 케이스를 먼저 차단
   - 결과 형식: PASS/WARN/FAIL, 점수, 누락 필드, 안전성 경고, 수정 요청 프롬프트

3. 앱 대시보드에 `에이전트 결과 표시 영역`을 추가한다.
   - 종목별 코칭 카드에 검증 상태를 추가한다.
   - 점수와 액션 옆에 리서치 요약, 애널리스트 의견, 운영자 리밸런싱 의견을 표시한다.
   - 검증 결과가 WARN/FAIL이면 대시보드에서 경고 상태로 보여준다.

4. 에이전트 실행 결과의 데이터 스키마를 확정한다.
   - `researchResult`
   - `analystResult`
   - `portfolioOperationResult`
   - `tradingResult`
   - `validationResult`
   - 이 스키마가 확정되어야 추후 서버, 카톡 연동, 자동 저장 기능을 안정적으로 붙일 수 있다.

5. 서버형 자동화 구조를 설계한다.
   - 카톡 오픈채팅방에서 직접 이미지를 받는 자동 봇은 카카오 정책상 제약이 있을 수 있으므로, 현실적인 1차안은 공개 HTTPS 앱 링크에서 이미지를 업로드하고 결과를 카톡에 공유하는 방식이다.
   - 2차안은 서버가 이미지 업로드, OCR, 에이전트 실행, 세션 저장, 공유 링크 생성을 처리하는 구조다.
   - 서버 도입 시 원본 이미지 자동 삭제, API 키 환경 변수 관리, 요청 제한, 개인정보 마스킹이 필수다.

추천 바로 다음 작업:

- 먼저 `1개 종목 end-to-end 에이전트 실행 샘플`을 문서로 만들고, 그 결과를 현재 대시보드가 받을 수 있는 JSON 구조로 정리한다.
- 그 다음 앱에 `에이전트 분석 결과` 섹션을 추가한다.
- 마지막으로 테스트 시나리오 3개를 실제 검증 관리자 방식으로 채점해 회귀 테스트 기준으로 삼는다.

`삼성전자 1개 종목 end-to-end 에이전트 실행 샘플`을 생성했다.

- 실행 샘플 문서: `docs/agent-run-samples/2026-07-17-samsung-electronics-e2e.md`
- 실행 샘플 JSON: `docs/agent-run-samples/2026-07-17-samsung-electronics-e2e.json`
- 공통 스키마 문서: `docs/schemas/agent-analysis-result.md`
- 실행 흐름: 보유 종목 데이터 → 주식 리서치 전문가 → 주식 애널리스트 → 포트폴리오 운영자 → 에이전트 검증 관리자 → 대시보드 표시값
- 샘플 결론: 기존 앱 점수는 `57점/보유`이나, 포트폴리오 비중 40% 초과와 큰 평가이익을 반영해 대시보드 최종 표시값은 `44점/분할매도 1단계`로 보수 조정
- 검증 상태: `WARN`
- WARN 사유: 공식 출처와 기준일은 포함했지만, 목표가와 매수/매도 구간은 2Q26 확정 실적 전 간이 시나리오이므로 추후 EPS, 피어 멀티플, 컨센서스 목표가 확인 필요
- 다음 단계: 앱의 종목별 내용 영역에 `agentAnalysis` 결과를 표시하고, WARN/FAIL 검증 상태를 뱃지로 노출한다.

## 2026-07-23 프로젝트 로컬 스킬 정의

에이전트들이 공통으로 사용할 mystock 전용 로컬 스킬을 `skills/` 아래에 정의했다.

| 스킬 | 주 사용 에이전트 | 목적 |
|---|---|---|
| `stock-research-workflow` | 주식 리서치 전문가 | 기업, 뉴스, 공시, 성장성, 리스크 조사 |
| `stock-analyst-reporting` | 주식 애널리스트 | 애널리스트 리포트, 투자의견, 목표가, 가격 구간 작성 |
| `portfolio-rebalancing` | 포트폴리오 운영자 | 목표 비중, 리밸런싱 액션, 현금 계획 수립 |
| `trading-plan-generation` | 데이트레이딩, 스윙, 프라이스 액션 스윙 | 진입, 청산, 손절, 트레일링 스톱 계획 작성 |
| `agent-response-validation` | 에이전트 검증 관리자 | 답변의 근거, 안전성, 필수 필드, 대시보드 사용 가능성 검증 |
| `agent-test-scenario-design` | 에이전트 테스트 시나리오 생성기 | 정상/경계/위험/안전성 테스트 시나리오 생성 |
| `stock-coach-agent-chain` | 전체 에이전트 체인 | 보유 종목 데이터에서 대시보드 JSON까지 end-to-end 실행 |

메모:

- 이 스킬들은 전역 Codex 스킬이 아니라 프로젝트 내부 로컬 스킬이다.
- 에이전트 상세 역할은 `docs/agents/*.md`에 두고, 반복 실행 절차는 `skills/*/SKILL.md`에 둔다.
- 공식 `quick_validate.py` 검증은 로컬 Python 환경에 `yaml` 모듈이 없어 실행하지 못했다. 대신 `SKILL.md` frontmatter 구조와 기존 앱 테스트를 별도로 확인한다.
- 각 스킬에는 `agents/openai.yaml` UI 메타데이터도 추가했다.

## 2026-07-23 여러 종목 에이전트 분석 JSON 생성기

여러 종목 입력 JSON을 받아 에이전트 분석 결과 JSON을 만드는 1차 생성기를 구현했다.

- 핵심 함수: `generateAgentAnalysisResults(input)`
- 위치: `src/portfolio.js`
- CLI: `scripts/generate-agent-analysis.mjs`
- npm 명령: `npm run agent:generate -- <input.json> [output.json]`
- 테스트: `test/portfolio.test.js`
- 샘플 입력: `docs/agent-run-samples/2026-07-23-multi-holdings-input.json`
- 샘플 출력: `docs/agent-run-samples/2026-07-23-multi-holdings-output.json`

생성 흐름:

1. 입력 JSON의 `portfolio[]`를 정규화한다.
2. 비중이 비어 있으면 평가금액 기준으로 자동 계산한다.
3. 기존 앱 점수는 `appScore`에 보존한다.
4. 로컬 스킬 규칙을 기준으로 `researchResult`, `analystResult`, `portfolioOperationResult`, `validationResult`를 만든다.
5. 대시보드 표시용 `dashboardResult`를 만든다.
6. 여러 종목 결과는 `results[]` 배열로 반환한다.

현재 한계:

- 실제 Workspace Agent 또는 외부 LLM을 호출하지 않는다.
- 최신 뉴스/공시/실적/컨센서스는 자동 조회하지 않는다.
- 따라서 기본 검증 상태는 `WARN`이며, 대시보드 사용은 가능하지만 최신 출처 확인이 필요하다.

다음 단계:

- 현재 웹앱에 JSON 업로드/붙여넣기 입력 영역을 추가한다.
- 생성된 `dashboardResult`와 `agentAnalysis`를 종목별 `내용` 영역에 표시한다.
- 이후 서버를 붙이면 최신 데이터 조회와 실제 에이전트 호출로 `WARN`을 `PASS`로 올릴 수 있다.

## 보안 및 개인정보 원칙

- 업로드 이미지는 기본적으로 브라우저 안에서 처리한다.
- 별도 서버 전송은 사용자가 명시적으로 동의한 기능에서만 허용한다.
- 이미지 파일은 허용 확장자와 용량을 검증한다.
- API 키는 코드에 넣지 않고 환경 변수 또는 설정 저장소를 사용한다.
